name: Production release

on:
  workflow_dispatch: {}

jobs:
  approval:
    type: require-approval
  build_android:
    type: build
    needs: [approval]
    params:
      platform: android
      profile: production
  build_ios:
    type: build
    needs: [approval]
    params:
      platform: ios
      profile: production
