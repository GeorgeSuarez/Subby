# Settings screen: use the route instead of a fixed point over the native tab bar.
appId: host.exp.Exponent
---
- openLink: exp://127.0.0.1:8081/--/settings
- extendedWaitUntil:
    visible: 'Theme'
    timeout: 30000
- takeScreenshot: settings
