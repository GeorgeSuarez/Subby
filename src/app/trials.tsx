/**
 * Active trials route.
 *
 * Thin re-export of the `trials` feature screen. Registered in the root stack
 * with a native header; pushed from the dashboard's TrialsCard ("View all").
 */

export { TrialsScreen as default } from '@/features/trials';
